name: Python-Jupyter

on:
  pull_request:
    branches:
    - master

jobs:
  Build-Docker:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: docker build ./jupyter/ --tag joachimveulemans-build-test:$(date +%s)
